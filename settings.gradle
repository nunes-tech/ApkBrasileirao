pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //Dependencia do GlideToVectorYou para carregar Svg
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Brasileirao"
include ':app'
